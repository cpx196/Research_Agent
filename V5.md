# Research Agent V5：MCP 工具标准化与 Skill 能力封装任务书

## 0. 版本定位

V5 在现有版本基础上继续开发：

```text
V0/V2：
手写 ReAct + Tool Calling + RAG

V3：
LangGraph Workflow

V4：
Context Manager / 上下文压缩

V5：
MCP + Tool Abstraction + Skills
```

V5 的核心问题不是：

```text
如何再增加几个 Tool
```

而是：

> **如何把 Agent 的外部能力从“项目内部手写 Python 函数”升级成标准化、可插拔、可扩展的工具层。**

---

# 0.1 当前阶段开发原则

从 V5 开始，当前阶段统一采用：

```text
先完成方案实现
→ 做最小功能验证
→ 保证旧版本不被破坏
→ 暂不做大规模 Benchmark
```

详细的：

```text
成功率
延迟
Token
消融
多版本横向对比
```

统一留到最终评测阶段。

---

# 1. 强制兼容规则

继续遵守项目统一原则：

```text
1. 不删除旧版本
2. 不删除现有 Native Python Tools
3. 不删除 Tool Registry
4. 不删除 V2 RAG
5. 不删除 V3 LangGraph
6. 不删除 V4 Context Manager
7. MCP 作为新增路径，而不是覆盖旧路径
```

必须能够运行：

```bash
python main.py --mode legacy
python main.py --mode graph_baseline
python main.py --mode graph_context
python main.py --mode graph_mcp
```

如果当前 CLI 结构不同，可以采用等价配置。

---

# 2. 当前工具结构

目前工具主要是：

```text
calculator
web_search
paper_search
local_search
```

它们本质上都是：

```python
def tool(...):
    ...
```

然后注册到：

```python
TOOL_REGISTRY
```

Agent 通过 Tool Schema 决定调用。

当前模式：

```text
LLM
 ↓
Tool Call
 ↓
Tool Registry
 ↓
Python Function
 ↓
Result
```

这个方案本身没有问题。

V5 不替换它。

---

# 3. 为什么需要 MCP

随着工具数量增加，例如未来要接：

```text
GitHub
文件系统
数据库
浏览器
代码执行环境
远程服务
企业内部 API
```

如果全部使用：

```text
一个服务
→ 手写一个 Python Wrapper
→ 手写 Schema
→ 手写连接逻辑
→ 手写生命周期
```

工具层会越来越难维护。

MCP 的作用是：

> **为 LLM 应用与外部能力之间提供统一协议。**

目标结构：

```text
Agent
  ↓
Unified Tool Layer
  ↓
┌─────────────────────┐
│                     │
▼                     ▼
Native Tools        MCP Client
                      ↓
                   MCP Server
                      ↓
            Tools / Resources / Prompts
```

---

# 4. MCP 全称

```text
Model Context Protocol
```

可以先简单理解为：

```text
LLM 应用与外部工具 / 数据之间的标准接口协议
```

它不是：

```text
LLM
Agent 模型
RAG
LangGraph
```

它解决的是：

```text
外部能力如何标准化接入
```

---

# 5. MCP 的基本角色

V5 必须理解三个角色。

## 5.1 Host

Host 是整个 LLM 应用。

在本项目里：

```text
Research Agent
```

就是 Host。

---

## 5.2 MCP Client

Client 运行在 Host 内部。

作用：

```text
连接 MCP Server
列出能力
调用 Tool
读取 Resource
获取 Prompt
处理返回结果
```

结构：

```text
Research Agent
      ↓
   MCP Client
```

---

## 5.3 MCP Server

Server 对外暴露能力。

例如：

```text
GitHub MCP Server
Filesystem MCP Server
Research MCP Server
```

Server 可以提供：

```text
Tools
Resources
Prompts
```

---

# 6. MCP 三种核心能力

本版本重点理解：

```text
Tool
Resource
Prompt
```

注意三者用途不同。

---

# 7. MCP Tool

Tool 是：

> 模型可以主动决定调用的操作。

例如：

```text
search_papers
read_file
search_repo
query_database
```

和目前 V0 Tool Calling 最接近。

流程：

```text
LLM
 ↓
决定调用 Tool
 ↓
MCP Client
 ↓
MCP Server
 ↓
执行 Tool
 ↓
Result
 ↓
LLM
```

因此可以理解：

```text
MCP Tool
≈
标准化后的外部 Tool
```

---

# 8. MCP Resource

Resource 更适合：

> 读取数据 / 上下文，而不是执行动作。

例如：

```text
某个本地文件
某篇文档
数据库中的只读内容
项目配置
某个静态资源
```

可以简单类比：

```text
Tool
≈ 做事情

Resource
≈ 取数据
```

本版本不要求大量使用 Resource，但必须实现至少一个示例。

---

# 9. MCP Prompt

Prompt 是：

> MCP Server 提供的可复用提示模板。

例如：

```text
review_paper
compare_models
summarize_repository
```

本版本只要求理解并实现一个简单示例。

注意：

```text
MCP Prompt
≠ Agent System Prompt
```

它只是 Server 可以暴露的一种标准能力。

---

# 10. 当前 MCP SDK

V5 优先使用官方 Python MCP SDK。

建议环境：

```text
Python >= 3.10
mcp
```

当前官方 Python SDK 支持：

```text
MCP Server
MCP Client
Tools
Resources
Prompts
stdio
Streamable HTTP
```

本版本优先使用：

```text
stdio
```

完成本地实验。

后续再测试：

```text
Streamable HTTP
```

---

# 11. 为什么先使用 stdio

stdio 模式：

```text
Agent
 ↓
启动 MCP Server 子进程
 ↓
stdin / stdout 通信
```

优点：

```text
本地开发简单
不需要端口
不需要部署服务器
适合验证 MCP Protocol
```

本阶段重点是：

```text
理解 MCP
```

而不是网络部署。

---

# 12. V5 第一阶段：自己实现一个 MCP Server

不要一开始直接接几十个第三方 MCP Server。

先将现有工具中的一部分包装成：

```text
Research MCP Server
```

建议暴露：

```text
calculator
paper_search
```

例如：

```text
research_mcp_server/
├── server.py
└── tools.py
```

目标：

```text
Agent
 ↓
MCP Client
 ↓
Research MCP Server
 ↓
calculator
paper_search
```

---

# 13. 为什么先包装已有 Tool

目的不是新增能力。

而是做严格对照：

```text
Native calculator
vs
MCP calculator
```

底层逻辑完全相同。

这样可以测：

```text
协议额外延迟
开发复杂度
异常处理
Tool Schema 获取方式
Tool 调用流程
```

避免把：

```text
工具能力差异
```

误认为：

```text
MCP 差异
```

---

# 14. Native Tool 与 MCP Tool 共存

推荐统一抽象：

```python
class ToolAdapter:
    name: str
    description: str

    async def call(self, **kwargs):
        ...
```

实现：

```text
NativeToolAdapter

MCPToolAdapter
```

上层 Agent 不关心：

```text
Tool 到底来自 Python Function
还是 MCP Server
```

最终：

```text
Agent
 ↓
Unified Tool Registry
 ↓
┌────────────────┬────────────────┐
▼                ▼
Native Tool     MCP Tool
```

---

# 15. 不删除现有 TOOL_REGISTRY

原有：

```python
TOOL_REGISTRY = {
    "calculator": calculator,
    "web_search": web_search,
    "paper_search": paper_search,
    "local_search": local_search,
}
```

必须保留。

新增：

```text
MCP_TOOL_REGISTRY
```

或者统一：

```python
UNIFIED_TOOL_REGISTRY = {
    ...
}
```

但原始 Registry 不得删除。

---

# 16. MCP Client Manager

新增：

```text
mcp/
├── client.py
├── manager.py
├── adapters.py
├── registry.py
└── config.py
```

建议：

```python
class MCPClientManager:

    async def connect(self):
        ...

    async def list_tools(self):
        ...

    async def call_tool(self, name, arguments):
        ...

    async def list_resources(self):
        ...

    async def read_resource(self, uri):
        ...

    async def close(self):
        ...
```

重点处理：

```text
连接生命周期
Server capability discovery
Tool discovery
Tool call
Timeout
Error
Shutdown
```

---

# 17. Tool Discovery

V0 的 Tool Schema 是我们手动提供的。

V5 要理解一个重要区别：

```text
Native Tool：
我们自己维护 Schema

MCP Tool：
Client 可以从 Server 查询 Tool 描述
```

流程：

```text
MCP Client
 ↓
list_tools()
 ↓
Tool Name
Description
Input Schema
```

然后转换成当前 LLM Tool Calling 可以使用的格式。

---

# 18. MCP Tool → LLM Tool Schema Adapter

需要新增 Adapter：

```text
MCP Tool Schema
      ↓
Schema Adapter
      ↓
LLM Tool Schema
```

例如最终仍然给 LLM：

```json
{
  "type": "function",
  "function": {
    "name": "...",
    "description": "...",
    "parameters": {...}
  }
}
```

Agent 的 Tool Calling 机制不用改变。

---

# 19. V5 最重要的理解

MCP 不改变：

```text
LLM 是否决定调用 Tool
```

仍然是：

```text
LLM
↓
Tool Call
```

MCP 只改变：

```text
Tool 怎么发现
Tool 怎么连接
Tool 怎么执行
Tool 怎么返回
```

所以：

```text
Agent Intelligence
≠ MCP

Tool Infrastructure
≈ MCP
```

---

# 20. MCP Error Handling

至少处理：

```text
Server 启动失败
连接失败
Tool 不存在
Tool 参数错误
Tool Timeout
Server 崩溃
返回 is_error
Protocol Error
```

要求：

```text
MCP 单个 Tool 失败
不能导致整个 Graph 崩溃
```

返回统一：

```text
ToolError
```

然后交给 Agent 判断：

```text
Retry
Fallback
换 Tool
结束
```

---

# 21. Native Tool Fallback

V5 建议实现：

```text
MCP Tool
 ↓
失败
 ↓
是否存在 Native Equivalent?
 ↓
Yes
 ↓
Native Tool
```

例如：

```text
MCP calculator
↓
Server failure
↓
Native calculator
```

这可以作为一个可靠性实验。

---

# 22. 第三方 MCP Server

完成自建 MCP Server 后，再接至少一个外部 MCP Server。

优先选择：

```text
Filesystem
GitHub
```

如果当前环境没有稳定的第三方 Server，可以先：

```text
自建 Filesystem MCP Server
```

不要为了完成 V5 强行接不可靠服务。

---

# 23. 推荐实验：Filesystem MCP

增加：

```text
list_files
read_file
search_files
```

但必须：

```text
限定允许访问的目录
```

例如只允许：

```text
Research_Agent/
papers/
evaluation/
```

禁止默认允许整个系统文件访问。

---

# 24. GitHub MCP 可选实验

如果可用，可以增加：

```text
search_repository
read_repository_file
search_issues
```

用于 Research Agent 调研开源项目。

例如：

```text
帮我检查 V-JEPA 2 官方仓库是否提供 checkpoint
```

Agent 可以：

```text
GitHub MCP Tool
```

而不是普通 Web Search。

---

# 25. Skills 的定位

V5 中的 Skill 不定义为 MCP 标准的一部分。

在本项目里：

> **Skill = 多个底层 Tool + 一套明确流程形成的高层能力。**

例如：

```text
Tool：
paper_search
web_search
local_search
github_search

Skill：
investigate_paper()
```

---

# 26. Skill 示例：论文调研

新增：

```text
skills/
├── paper_research.py
└── repo_research.py
```

例如：

```text
Paper Research Skill
```

内部流程：

```text
输入论文名
 ↓
local_search
 ↓
paper_search
 ↓
需要时 web_search
 ↓
提取：
标题
作者
时间
方法
开源状态
checkpoint
 ↓
结构化结果
```

---

# 27. Skill 和 Tool 的区别

必须在 README 解释：

```text
Tool
=
原子操作
```

例如：

```text
search_web(query)
```

而：

```text
Skill
=
一组 Tool + 工作流程
```

例如：

```text
investigate_open_source_status(project)
```

可能内部调用：

```text
web_search
github_search
paper_search
```

---

# 28. Skill 和 Agent 的区别

Skill 通常是：

```text
目标明确
流程相对固定
输出格式明确
```

Agent 则可以：

```text
动态决策
动态 Tool Calling
动态规划
```

因此：

```text
Skill
可以被 Agent 当成高层 Tool 使用
```

---

# 29. Skill 设计原则

V5 只实现 1~2 个 Skill。

不要做 Skill 大合集。

建议：

```text
Paper Research Skill

Open-source Repo Inspection Skill
```

重点理解：

```text
什么时候应该暴露底层 Tool
什么时候应该把一组 Tool 封装成 Skill
```

---

# 30. 推荐整体架构

```text
                         Agent
                           │
                           ▼
                  Unified Capability Layer
                           │
          ┌────────────────┼─────────────────┐
          ▼                ▼                 ▼
     Native Tools       MCP Tools          Skills
          │                │                 │
          │            MCP Client            │
          │                ↓                 │
          │            MCP Server            │
          │                                  │
          └────────────────┬─────────────────┘
                           ▼
                        Result
                           ↓
                       Evidence
                           ↓
                    Context Manager
                           ↓
                         LLM
```

---

# 31. 与 LangGraph 的关系

LangGraph 仍然负责：

```text
Planner
Researcher
Writer
Verifier
Routing
State
```

MCP 不替代 LangGraph。

结构：

```text
LangGraph
   ↓
Researcher Node
   ↓
Tool Calling
   ↓
Unified Tool Layer
   ↓
Native / MCP
```

---

# 32. 与 V4 Context Manager 的关系

MCP Tool Result 也必须进入现有：

```text
Tool Result Compression
Evidence Extraction
Context Manager
```

不能因为是 MCP Tool 就直接把大段 Result 全塞给 LLM。

统一处理：

```text
Native Tool Result
             \
              → Evidence Pipeline
             /
MCP Tool Result
```

---

# 33. Trace

V5 Trace 必须新增：

```text
[Tool Source]
Native / MCP / Skill

[MCP Server]
server name

[MCP Tool]
tool name

[Transport]
stdio / HTTP

[Latency]
xxx ms

[Fallback]
Yes / No
```

例如：

```text
[Researcher]

Tool Source:
MCP

Server:
research-server

Tool:
paper_search

Arguments:
{
  "query": "V-JEPA 2"
}

MCP Latency:
42 ms

Result:
...
```

---

# 34. 当前阶段测试原则

V5 当前阶段不做大规模 Benchmark、消融实验或系统性能评测。

本阶段只做最小验证，确认：

```text
1. MCP Server 能启动
2. MCP Client 能连接
3. 能发现 Tool
4. 能成功调用至少一个 MCP Tool
5. MCP Tool 能被 Agent 正常使用
6. Native Tool 仍然可用
7. MCP 失败时不会导致整个 Agent 崩溃
8. Skill 至少能完整跑通一个示例
```

目标是：

```text
先把功能链路搭完整
```

而不是：

```text
现在就证明 MCP / Skill 一定优于旧方案
```

---

# 35. 最小 Smoke Test

至少完成：

## MCP 基础

```text
启动 MCP Server
→ Client 连接成功
→ list_tools() 正常
→ call_tool() 正常
```

## Agent 接入

```text
用户问题
→ LLM Tool Call
→ MCP Tool
→ Result
→ Agent Final Answer
```

## Skill

至少跑通一个：

```text
Paper Research Skill
```

确认：

```text
Skill
→ 调用底层 Tool
→ 返回结构化结果
```

---

# 36. 最小回归测试

V5 完成后只需要确认旧版本没有被破坏：

```text
Legacy ReAct
V3 LangGraph
V4 Context Manager
V5 MCP
```

至少分别跑 1~2 个简单任务。

重点确认：

```text
calculator 正常
local_search 正常
LangGraph 正常
Context Manager 正常
MCP 正常
```

---

# 37. 最终统一评测：暂缓

以下内容全部保留为最终阶段待办，但 V5 当前不执行：

```text
Native vs MCP 延迟对比
MCP Overhead
Tool Success Rate
Fallback Rate
Fallback Success Rate
Skill vs Raw Tool Chain
LLM Calls
Tool Calls
Input Tokens
Total Tokens
Total Latency
Task Success Rate
Evidence Coverage
大规模 Benchmark
消融实验
```

最终项目完成后统一进行：

```text
V0/V2
V3
V4
V5
V6...
```

横向对比。

---

# 38. 后续统一测试建议

最终评测阶段再比较：

```text
A：Native Tools

B：MCP Tools

C：MCP + Native Fallback

D：Raw Tool Chain

E：Skill
```

届时统一统计：

```text
Task Success
Tool Success
Latency
Token
LLM Calls
Tool Calls
Recovery
Engineering Cost
```

---

# 39. V5 当前不追求

V5 当前不要求证明：

```text
MCP 比 Native Tool 更快
Skill 比原始 Tool Chain 更好
```

当前只要求：

```text
MCP 链路正确
Tool Discovery 正确
Tool Calling 正确
旧 Tool 保留
Skill 能工作
```

性能与效果结论留到最终统一测试。

---

# 44. 开发量对比

需要额外记录一个工程指标：

```text
新增一个 Tool 需要修改多少文件 / 代码
```

例如：

### Native Tool

```text
实现函数
Schema
Registry
Error handling
```

### MCP Tool

```text
Server 暴露
Client 自动发现
```

尝试定性/定量比较：

```text
Integration LOC
Modified Files
Configuration Changes
```

---

# 45. 推荐目录

```text
Research_Agent/
│
├── agent/
│   ├── legacy/
│   ├── graph/
│   └── context/
│
├── tools/
│   ├── calculator.py
│   ├── web_search.py
│   ├── paper_search.py
│   ├── local_search.py
│   └── registry.py
│
├── mcp_layer/
│   ├── client.py
│   ├── manager.py
│   ├── adapters.py
│   ├── registry.py
│   ├── config.py
│   │
│   └── servers/
│       └── research_server.py
│
├── skills/
│   ├── paper_research.py
│   └── repo_research.py
│
├── evaluation/
│   ├── v5_results.md
│   ├── v5_native_vs_mcp.md
│   ├── v5_skill_ablation.md
│   └── v5_traces.md
│
└── tests/
    ├── test_mcp_client.py
    ├── test_mcp_server.py
    ├── test_mcp_adapter.py
    ├── test_mcp_fallback.py
    └── test_skills.py
```

---

# 46. MCP 配置

建议：

```yaml
mcp_servers:
  research:
    transport: stdio
    command: python
    args:
      - mcp_layer/servers/research_server.py
    enabled: true
```

或者等价 JSON / Python 配置。

不要把：

```text
Server 地址
命令
权限
```

硬编码散落在 Agent Node 内。

---

# 47. 权限控制

V5 必须考虑 Tool 权限。

尤其 Filesystem Tool。

要求：

```text
只允许访问白名单目录
```

例如：

```text
papers/
evaluation/
README.md
```

禁止默认：

```text
/
~/ 全目录
系统配置目录
```

---

# 48. Side Effect Tool

如果未来接入：

```text
写文件
创建 Issue
发送请求
```

需要区别：

```text
Read-only Tool
Side-effect Tool
```

V5 第一版尽量优先：

```text
Read-only
```

避免扩大安全面。

---

# 49. 单元测试

至少新增：

```text
test_mcp_server.py
test_mcp_client.py
test_mcp_adapter.py
test_mcp_fallback.py
test_skill.py
```

覆盖：

```text
Server 启动
Tool Discovery
Tool Call
参数错误
Server Error
Timeout
Fallback
Skill
```

---

# 50. 回归测试

完成 V5 后只做最小回归：

```text
Legacy
V3
V4
V5
```

确认：

```text
旧 Native Tool 仍可用
RAG 正常
LangGraph 正常
Context Manager 正常
MCP 正常
```

每个版本 1~2 个简单任务即可。

不要在本阶段做大规模性能测试。

---

# 51. 完成后必须输出

本地 Agent 完成 V5 后提交：

```text
1. 最终目录树

2. MCP Server 实现

3. MCP Client 实现

4. MCP Tool Discovery 过程

5. MCP → LLM Schema Adapter

6. Unified Tool Layer

7. Native Tool 保留方式

8. Native Fallback 实现

9. MCP Resource 示例

10. MCP Prompt 示例

11. Skill 实现

12. 1~2 个 MCP Smoke Test

13. 1 个 Skill Demo

14. 至少 2 条完整 V5 Trace

15. 最小旧版本 Regression Test

16. 当前已知问题

17. 最终统一评测待办清单
```

---

# 52. V5 必须理解的问题

完成后应该能够回答：

1. MCP 全称是什么？
2. MCP 解决的核心问题是什么？
3. Host / Client / Server 分别是什么？
4. MCP Tool 和 Native Python Tool 的区别是什么？
5. Tool / Resource / Prompt 有什么区别？
6. LLM 是否会因为使用 MCP 就变得更聪明？
7. Tool Calling 决策还是谁做？
8. MCP 为什么不替代 LangGraph？
9. MCP 为什么不替代 RAG？
10. 为什么 Tool Discovery 有价值？
11. stdio 和 HTTP Transport 有什么区别？
12. 为什么 Native Tool 还要保留？
13. 为什么需要 Fallback？
14. Skill 和 Tool 有什么区别？
15. Skill 是不是 MCP 协议的一部分？
16. 为什么不能默认让 Filesystem MCP 访问整个磁盘？
17. MCP 增加的主要价值是性能还是工程标准化？
18. 如何测试 MCP 是否真的值得引入？

---

# 53. 最终架构

```text
                         Research Agent
                               │
                               ▼
                         LangGraph
                               │
                               ▼
                          Researcher
                               │
                               ▼
                     Unified Capability Layer
                               │
             ┌─────────────────┼──────────────────┐
             ▼                 ▼                  ▼
        Native Tools        MCP Tools           Skills
             │                 │                  │
             │             MCP Client             │
             │                 ↓                  │
             │             MCP Server             │
             │          ┌──────┼──────┐           │
             │          ▼      ▼      ▼           │
             │        Tools Resources Prompts     │
             │                                    │
             └──────────────────┬─────────────────┘
                                ▼
                             Result
                                ↓
                       Evidence Extraction
                                ↓
                         Context Manager
                                ↓
                               LLM
```

---

# 54. V5 的核心价值

V0/V2：

```text
我们学会了 Agent 如何调用 Tool。
```

V3：

```text
我们学会了 Agent Workflow 如何组织。
```

V4：

```text
我们学会了如何控制长任务 Context。
```

V5：

```text
我们要解决 Tool 本身如何标准化、模块化和可扩展。
```

最终目标不是：

```text
“项目用了 MCP”
```

而是能够证明：

> **在保留原生 Tool Baseline 的基础上，引入 MCP 后，Agent 可以通过统一协议发现、连接和调用外部能力，并通过测试量化其协议开销、失败恢复能力、集成成本和扩展性；同时使用 Skill 将多 Tool 流程封装成可复用的高层能力。**
