# Research Agent V3：LangGraph Workflow 重构任务书

## 0. 强制版本兼容规则

从 V3 开始，项目必须遵守以下规则：

> **实现新方案时，不允许删除旧方案代码。旧方案必须保留，并通过注释、旁路、配置开关或独立入口的方式继续存在。**

本版本尤其要求保留 V0/V2 中已经跑通的手写 Agent Loop。

禁止：

```text
直接删除 V0 agent loop
直接覆盖旧文件导致无法回退
直接把旧实现替换成 LangGraph 后不保留旧入口
```

推荐：

```text
旧实现继续存在
+
新增 LangGraph 实现
+
通过配置选择运行模式
```

例如：

```bash
python main.py --mode legacy
python main.py --mode graph
```

如果原项目结构不适合 CLI 参数，也可以使用：

```text
main_legacy.py
main_graph.py
```

但必须保证旧方案仍然可以独立运行。

---

## 1. V3 项目目标

当前 V2 已经拥有：

```text
LLM
+
Tool Calling
+
calculator
+
web_search
+
paper_search
+
local_search
+
RAG
```

并通过手写循环运行：

```text
while True:
    LLM
    ↓
    Tool Call?
    ├─ Yes → Tool → Observation → LLM
    └─ No  → Final Answer
```

V3 的目标不是简单地把这段 `while` 换成 LangGraph。

V3 要完成两个目标：

### 目标 A：把 Agent 执行过程显式 Graph 化

学习：

```text
State
Node
Edge
Conditional Edge
ToolNode
Graph Loop
Checkpoint
```

理解 LangGraph 与 V0 手写 Agent Loop 的对应关系。

### 目标 B：加入真正的 Research Workflow

从：

```text
User
 ↓
LLM
 ↕
Tools
 ↓
Answer
```

升级成：

```text
User Query
     ↓
   Planner
     ↓
 Researcher
     ↓
    Tools
     ↓
  Evidence
     ↓
   Writer
     ↓
  Verifier
   /    \
Pass    Fail
 ↓       ↓
END   Researcher
```

使复杂 Research Task 不再完全依赖一个 LLM 在自由循环中临时决定全部步骤。

---

## 2. 为什么现在引入 LangGraph

V0 手写 Agent Loop 本质上已经具备：

```text
State + Decision + Tool Execution + Loop
```

但随着后续增加：

```text
Planner
Researcher
Writer
Verifier
Retry
Checkpoint
```

如果继续使用 `while / if / elif / continue / break` 控制复杂流程，代码会逐渐难以维护。

LangGraph 的核心作用是：

> **管理 Agent 的执行流程、状态、循环和条件分支。**

它不替代：

```text
LLM
Embedding Model
FAISS
RAG
Tools
Prompt
```

而是负责：

```text
这些模块按照什么顺序执行
什么时候循环
什么时候跳转
什么时候结束
共享什么状态
```

官方当前将 LangGraph 定义为用于 long-running、stateful agent 的低层 orchestration framework。

官方文档：

- https://docs.langchain.com/oss/python/langgraph/overview
- https://docs.langchain.com/oss/python/langgraph/graph-api

---

## 3. V0 与 LangGraph 的对应关系

| V0 手写 Agent | LangGraph |
|---|---|
| `messages` | State / MessagesState |
| `while` | Graph Loop |
| `if tool_calls` | Conditional Edge |
| `execute_tool()` | ToolNode / Tool Node |
| `continue` | Edge 回到某个 Node |
| `break` | END |
| `max_steps` | recursion / iteration control |
| 手动维护变量 | Shared State |
| 手写流程控制 | StateGraph |

因此可以把 V0 理解成：

```text
一个最小的、手写的 Agent Graph
```

V3 则把控制流显式化。

---

## 4. 本阶段核心技术

V3 需要掌握：

```text
LangGraph StateGraph
State
Node
Edge
Conditional Edge
START / END
ToolNode
Graph Compile
Graph Invoke
Graph Loop
Checkpoint
```

暂时不重点学习：

```text
Long-term Memory
Multi-Agent
Human-in-the-loop
LangSmith
GraphRAG
Deep Agents
Sub-Agent
```

---

## 5. State

State 是整个任务当前保存的信息。

V0 里主要只有：

```python
messages
```

V3 建议显式扩展成：

```python
class ResearchState(TypedDict):
    messages: list
    query: str
    plan: list[dict]
    current_step: int
    evidence: list[dict]
    draft: str
    verification_passed: bool
    verification_feedback: str
    iteration: int
```

可以理解成：

```text
ResearchState
=
用户问题
+
当前聊天消息
+
研究计划
+
已经搜到的 Evidence
+
当前答案 Draft
+
Verifier 结果
+
当前循环次数
```

所有 Node 都读取 State，并返回 State 的部分更新。

---

## 6. Node

Node 本质就是普通 Python Function。

```python
def planner_node(state: ResearchState):
    ...

def researcher_node(state: ResearchState):
    ...

def writer_node(state: ResearchState):
    ...

def verifier_node(state: ResearchState):
    ...
```

Node 可以内部调用 LLM、RAG、Tool 或普通 Python 代码。

LangGraph 并没有创造新的模型，它只是负责调度这些函数。

---

## 7. Edge

Edge 表示一个 Node 完成之后下一步去哪。

例如：

```text
Planner
↓
Researcher
```

概念代码：

```python
graph.add_edge("planner", "researcher")
```

---

## 8. Conditional Edge

Conditional Edge 表示动态分支。

例如：

```text
Verifier
   ↓
verification_passed?
   ├─ True  → END
   └─ False → Researcher
```

逻辑函数：

```python
def route_after_verification(state):
    if state["verification_passed"]:
        return "end"
    return "researcher"
```

这是 V3 需要重点理解的能力。

---

## 9. START / END

`START` 表示 Graph 入口，`END` 表示 Graph 终点。

例如：

```text
START
 ↓
Planner
 ↓
Researcher
 ↓
Writer
 ↓
Verifier
 ↓
END
```

---

## 10. V3 目标 Graph

推荐实现：

```text
                     START
                       ↓
                    Planner
                       ↓
                  Researcher
                       ↓
                 Research LLM
                       ↓
                 Tool Call?
                 /       \
              Yes         No
               ↓           ↓
           ToolNode      Evidence Ready
               ↓           ↓
          Research LLM    Writer
                             ↓
                          Verifier
                         /        \
                      Pass        Fail
                       ↓            ↓
                      END      Researcher
```

为了避免第一版过于复杂，也允许先拆成：

```text
START
  ↓
Planner
  ↓
Researcher
  ↓
Writer
  ↓
Verifier
  ↓
Pass?
 ├─ Yes → END
 └─ No  → Researcher
```

Researcher Node 内部继续复用 V2 已经实现的 Tool Calling 能力。

---

## 11. Planner Node

Planner 的职责是把复杂 Research Query 拆成明确研究步骤。

例如用户：

```text
比较 V-JEPA 2、DINOv3 和 JEPA-WAM 在机器人控制任务中的优缺点。
```

Planner 输出：

```text
1. 调研 V-JEPA 2 的 representation 训练目标
2. 调研 V-JEPA 2-AC 的机器人部分
3. 调研 DINOv3 representation 特点
4. 调研 JEPA-WAM 的结构和 action 部分
5. 比较三者语义能力
6. 比较三者动作建模方式
7. 汇总证据
```

建议 Planner 输出结构化数据：

```python
[
    {
        "id": 1,
        "task": "...",
        "status": "pending"
    }
]
```

不要只返回一段自由文本。

---

## 12. Researcher Node

Researcher 负责：

```text
执行 Research Plan
+
选择 Tool
+
收集 Evidence
```

必须复用现有工具：

```text
calculator
web_search
paper_search
local_search
```

不要重新实现这些工具。

---

## 13. Evidence

V3 开始必须显式维护 Evidence，而不是只把所有 Tool Result 混在 messages 中。

建议结构：

```python
{
    "source_type": "local_search",
    "source": "vjepa2.pdf",
    "page": 7,
    "query": "...",
    "content": "...",
    "score": 0.82
}
```

Web Evidence 可以是：

```python
{
    "source_type": "web_search",
    "title": "...",
    "url": "...",
    "content": "..."
}
```

目标职责分离：

```text
Planner      决定查什么
Researcher   获取 Evidence
Writer       基于 Evidence 写答案
Verifier     检查 Evidence 是否支持答案
```

---

## 14. Writer Node

Writer 的职责：

```text
Query + Plan + Evidence
↓
Draft Answer
```

Writer 不应主动搜索。

```text
Writer ≠ Researcher
```

将结果保存到：

```python
state["draft"]
```

---

## 15. Verifier Node

Verifier 输入：

```text
User Query
+
Evidence
+
Draft
```

判断：

```text
1. 是否回答用户问题
2. 关键结论是否有 Evidence 支撑
3. 是否存在明显遗漏
4. 是否需要继续 Research
```

输出：

```python
{
    "verification_passed": True,
    "verification_feedback": "..."
}
```

---

## 16. Verifier 条件路由

如果验证通过：

```text
Verifier → END
```

如果失败：

```text
Verifier → Researcher
```

并把 `verification_feedback` 作为下一轮 Research 的补充信息。

例如：

```text
缺少 DINOv3 在机器人任务中的直接证据。
```

Researcher 下一轮重点补这一项。

---

## 17. 最大循环次数

必须防止：

```text
Researcher → Writer → Verifier → Researcher → ...
```

无限循环。

State 中保留：

```python
iteration: int
```

建议：

```text
max_verification_iterations = 2 或 3
```

达到上限后停止继续 Research，输出当前最佳答案，并注明 Evidence 不完整。

---

## 18. ToolNode

如果当前版本支持稳定 `ToolNode`，可以使用。

其作用类似 V0 的：

```python
execute_tool()
```

也就是：

```text
读取 Tool Call
↓
找到 Tool
↓
执行
↓
生成 Tool Result
↓
写回 Messages / State
```

但必须保留旧：

```python
execute_tool()
TOOL_REGISTRY
```

README 需要说明：

```text
ToolNode ≈ V0 execute_tool() 的框架化实现
```

---

## 19. 不允许删除旧 Tool Registry

原有：

```python
TOOL_REGISTRY = {
    "calculator": calculator,
    "web_search": web_search,
    "paper_search": paper_search,
    "local_search": local_search,
}
```

必须保留。

同一批 Tool 同时服务：

```text
Legacy Agent
+
LangGraph Agent
```

---

## 20. 建议目录结构

```text
Research_Agent/
│
├── main.py
│
├── agent/
│   ├── agent.py
│   ├── llm.py
│   │
│   ├── legacy/
│   │   └── react_agent.py
│   │
│   └── graph/
│       ├── state.py
│       ├── graph.py
│       ├── routing.py
│       │
│       └── nodes/
│           ├── planner.py
│           ├── researcher.py
│           ├── writer.py
│           └── verifier.py
│
├── tools/
│   ├── calculator.py
│   ├── web_search.py
│   ├── paper_search.py
│   └── local_search.py
│
├── rag/
│   └── ...
│
├── evaluation/
│   ├── results.md
│   ├── traces_v2.md
│   └── traces_v4.md
│
└── tests/
    ├── ...
    └── test_graph.py
```

如果迁移旧文件风险较高，不要移动旧文件，直接新增 `agent/graph/`。

优先保证旧版本可运行。

---

## 21. 旧代码保留方式

### 方法 1：旧代码独立保留（推荐）

```text
legacy_agent.py
langgraph_agent.py
```

不需要真的把整段旧代码全部注释掉。

只要：

```text
旧实现没有被删除
+
仍然可以运行
```

即可。

### 方法 2：配置切换

```bash
python main.py --mode legacy
python main.py --mode graph
```

### 方法 3：同文件注释保留

如果必须在同一文件改写：

```python
# ===== V0 LEGACY IMPLEMENTATION =====
#
# def run_legacy(...):
#     while True:
#         ...
#
# ====================================
```

但优先推荐独立文件，而不是大量注释代码。

---

## 22. 禁止行为

本版本不允许：

```text
删除 V0 Agent Loop
删除 V2 RAG
重新实现一套 local_search
修改 BGE / FAISS 基础行为
同时大幅优化 RAG
加入 Multi-Agent
加入复杂 Memory
加入 MCP
```

V3 只聚焦：

```text
Workflow / Orchestration
```

---

## 23. Checkpoint

V3 加入最基础的 Checkpoint 概念。

目标：Graph 能保存 State Snapshot。

例如：

```text
Planner 完成
↓
State Snapshot

Researcher 完成
↓
State Snapshot

Writer 完成
↓
State Snapshot
```

第一版优先使用 InMemory Checkpointer。

如果实现稳定，可以再使用 SQLite Checkpointer。

本阶段只需理解：

```text
Checkpoint = Graph State 的运行快照
```

---

## 24. Trace

V3 必须生成比 V2 更清晰的 Graph Trace。

示例：

```text
[Graph]
START

[Node]
Planner

Plan:
1. ...
2. ...

[Edge]
Planner -> Researcher

[Node]
Researcher

Tool:
local_search

Query:
V-JEPA 2 action-conditioned robotics

[Evidence]
Source:
vjepa2.pdf
Page:
...

[Edge]
Researcher -> Writer

[Node]
Writer
Draft generated.

[Edge]
Writer -> Verifier

[Node]
Verifier
Passed:
False

Feedback:
Missing evidence about DINOv3.

[Conditional Edge]
Verifier -> Researcher
```

最终必须能看出：

```text
Node
State Update
Edge
Tool Call
Evidence
Verifier Result
```

---

## 25. 基础测试

### Test 1：简单问题

```text
什么是 Transformer？
```

验证 Graph 能正常结束。

### Test 2：单 Tool Research

```text
根据本地论文解释 V-JEPA 2-AC。
```

期望：

```text
Planner
→ Researcher
→ local_search
→ Writer
→ Verifier
→ END
```

### Test 3：复杂多源 Research

```text
比较 V-JEPA 2 和 DINOv3 在机器人控制中的潜在优势。
```

期望多 research step，并使用 local_search / paper_search 等工具。

### Test 4：Verifier Fail

故意构造 Evidence 不完整的问题。

要求至少触发一次：

```text
Verifier → Fail → Researcher
```

### Test 5：最大循环次数

构造无法取得充分 Evidence 的问题，验证达到最大 iteration 后正常结束。

---

## 26. Legacy Regression Test

这是 V3 强制测试。

实现 LangGraph 后，必须重新运行旧 V0/V2 Agent。

至少确认：

```text
calculator
web_search
paper_search
local_search
```

仍然正常。

要求：

```text
旧 Agent 仍可运行
```

---

## 27. Graph vs Legacy 对照

V3 至少做 5 个相同 Query。

比较：

| Metric | Legacy ReAct | LangGraph Workflow |
|---|---:|---:|
| Task Success | | |
| Tool Calls | | |
| LLM Calls | | |
| Retrieval Calls | | |
| Total Steps | | |
| Latency | | |
| Final Answer Quality | | |

暂时不要求 V3 一定优于 Legacy。

重点是建立 Baseline。

---

## 28. V3 不追求的东西

当前可能出现：

```text
LangGraph 更慢
LLM Call 更多
Token Cost 更高
```

这是允许的。

V3 的价值首先是：

```text
可控
可解释
可扩展
可追踪
```

后续再研究：

```text
怎么减少步骤
怎么降低成本
怎么提高成功率
```

---

## 29. 代码质量要求

必须：

- 保留旧 Agent
- 保留旧 Tool Registry
- 保留现有 RAG
- LangGraph 单独模块化
- State 定义集中管理
- Node 分模块
- Routing 分模块
- 有最大循环限制
- 有 Trace
- 有测试
- 有异常处理

禁止：

```text
所有 Node 写在 main.py
所有 State 使用无结构 dict 到处修改
删除旧实现
```

---

## 30. 验收标准

### LangGraph 基础

- [ ] 安装并成功使用 LangGraph
- [ ] 使用 StateGraph
- [ ] 定义 ResearchState
- [ ] 使用 START / END
- [ ] 至少 4 个 Node
- [ ] 使用固定 Edge
- [ ] 使用 Conditional Edge
- [ ] Graph 可以 compile
- [ ] Graph 可以 invoke

### Workflow

- [ ] Planner 可生成 Plan
- [ ] Researcher 可执行 Plan
- [ ] Researcher 可调用现有 Tools
- [ ] Evidence 单独保存
- [ ] Writer 基于 Evidence 生成 Draft
- [ ] Verifier 能输出 Pass / Fail
- [ ] Fail 能回到 Researcher
- [ ] 有最大迭代次数

### Compatibility

- [ ] V0/V2 旧实现未删除
- [ ] Legacy Agent 仍然可以运行
- [ ] 原 Tool Registry 未删除
- [ ] 原 RAG 未删除
- [ ] 可以选择 Legacy / Graph 两种模式

### Observability

- [ ] 能看到 Node 执行顺序
- [ ] 能看到 Edge Routing
- [ ] 能看到 Tool Call
- [ ] 能看到 Evidence
- [ ] 能看到 Verifier Result
- [ ] 能看到 iteration

---

## 31. 完成后输出报告

完成 V3 后，必须提交：

```text
1. 最终目录树
2. Legacy Agent 入口
3. LangGraph Agent 入口
4. ResearchState 定义
5. Graph 结构
6. Planner Node
7. Researcher Node
8. Writer Node
9. Verifier Node
10. Conditional Edge 实现
11. Checkpoint 实现
12. ToolNode / Tool Execution 方案
13. Legacy Regression Test
14. 至少 5 个 Graph 测试结果
15. 至少 3 个完整 Graph Trace
16. Legacy vs Graph 对照表
17. 当前存在的问题
```

---

## 32. 本阶段必须理解的问题

完成之后需要能够回答：

1. LangGraph 主要替代了 V0 哪部分代码？
2. State 与原来的 `messages` 有什么关系？
3. Node 本质是什么？
4. Edge 本质是什么？
5. Conditional Edge 解决什么问题？
6. ToolNode 和原 `execute_tool()` 有什么关系？
7. 为什么 LangGraph 不替代 RAG？
8. 为什么 Planner / Writer / Verifier 要拆开？
9. Evidence 为什么应该从 messages 中独立出来？
10. 为什么 Verifier Fail 后可以回到 Researcher？
11. Graph 为什么需要最大循环次数？
12. Checkpoint 保存的是什么？
13. Workflow 和 ReAct Agent 的区别是什么？
14. 为什么要保留 Legacy Agent？
15. LangGraph 的主要价值是不是“让模型更聪明”？

第 15 个问题的正确方向：

```text
不是。

LangGraph 主要增强的是：
Orchestration
State Management
Control Flow
Persistence
Observability

而不是直接提高底层 LLM 智力。
```

---

## 33. V3 完成后的整体架构

```text
                         User
                          │
                          ▼
                       Planner
                          │
                          ▼
                     Researcher
                          │
              ┌───────────┼───────────┐
              ▼           ▼           ▼
        local_search  paper_search  web_search
              │           │           │
              └───────────┼───────────┘
                          ▼
                       Evidence
                          │
                          ▼
                        Writer
                          │
                          ▼
                       Verifier
                       /      \
                    Pass      Fail
                     │          │
                     ▼          │
                    END ◄──── Researcher
```

底层 RAG 保持不变：

```text
PDF
↓
Chunk
↓
BGE
↓
FAISS
↓
local_search
```

旧版也必须继续存在：

```text
Legacy V0/V2:

User
 ↓
LLM
 ↓
Tool Call
 ↓
Tool
 ↓
Observation
 ↓
LLM
```

---

## 34. V3 的核心价值

V0/V2 证明：

```text
我理解 Tool Calling
我能手写 Agent Loop
我能接 RAG
```

V3 要证明：

```text
我理解复杂 Agent 的 Workflow Orchestration
我知道如何显式管理 State
我知道如何设计条件路由
我知道如何将 Planning / Research / Writing / Verification 分离
我知道如何保留 Baseline 并做对照
```

最终不是：

```text
“我会 LangGraph”
```

而应该是：

> **我先手写了 ReAct Agent Loop，再使用 LangGraph 将复杂 Research Agent 重构为具有显式 State、Planning、Verification 和条件循环的可控 Workflow，同时保留旧版 Baseline 做回归和后续对照实验。**
