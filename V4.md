# Research Agent V4：上下文管理与压缩任务书

## 0. 版本原则

V4 在 V3 LangGraph Workflow 基础上开发。

必须遵守：

```text
1. 不删除 V3 旧实现
2. 不删除 V2 RAG
3. 不删除 V0/V2 Legacy ReAct Agent
4. 新方案必须独立保留
5. 必须能随时切换回旧版本做对照实验
```

建议运行模式：

```bash
python main.py --mode legacy
python main.py --mode graph_baseline
python main.py --mode graph_context
```

其中：

```text
legacy
= V0/V2 手写 ReAct

graph_baseline
= V3 原始 LangGraph

graph_context
= V4 上下文管理版本
```

---

# 1. V4 核心目标

V3 当前的问题是：

```text
Agent 执行步骤越来越多
        ↓
messages / Tool Result / Evidence 越积越多
        ↓
LLM 每次输入 Context 越来越长
        ↓
Prefill Token 增加
        ↓
延迟增加
        ↓
Token Cost 增加
        ↓
旧信息干扰当前任务
        ↓
可能重复搜索 / 跑偏
```

V4 的目标是：

> **不再把完整执行历史全部交给每个 Node，而是根据当前任务和当前 Node，只提供完成当前步骤所需的最小充分上下文。**

核心关注：

```text
Tool Result Compression
Evidence Selection
Node-specific Context
Context Budget
```

---

# 2. V4 不做什么

本版本明确不做：

```text
跨会话长期 Memory
用户画像
长期偏好记忆
跨天 / 跨任务记忆
Multi-Agent Memory
复杂知识图谱 Memory
长期向量数据库
```

本版本只处理：

> **单次 Research Task 内部的上下文管理。**

即：

```text
一个任务开始
↓
任务执行
↓
上下文不断增长
↓
动态压缩 / 选择
↓
任务结束
```

任务结束后不要求保存历史状态。

---

# 3. V4 要解决的核心问题

当前 V3 可能把以下全部放入 Context：

```text
System Prompt
User Query
Planner 输出
Researcher 消息
local_search 输出
web_search 输出
paper_search 输出
Tool Error
重试记录
Writer Draft
Verifier Feedback
第二轮检索
第二轮 Draft
...
```

如果任务较长：

```text
Step 1 → 3000 tokens
Step 5 → 7000 tokens
Step 10 → 12000 tokens
Step 20 → 20000+ tokens
```

V4 希望把 Active Context 稳定控制在合理范围。

---

# 4. V4 最终上下文结构

每个 Node 不再直接读取完整历史。

改成：

```text
                 Research State
                       ↓
                Context Manager
                       ↓
             Node-specific Context
```

最终 LLM 实际看到的内容：

```text
System Prompt
+
Current Query
+
Current Plan / Current Step
+
Open Questions
+
Relevant Evidence
+
Verifier Feedback（如有）
+
少量必要的 Recent Messages（可选）
```

而不是：

```text
全部 messages
+
全部 Tool Results
```

---

# 5. ResearchState 保持结构化

建议继续使用 V3 State，并增加 Context 相关字段：

```python
class ResearchState(TypedDict):
    messages: list

    query: str

    plan: list
    current_step: int

    evidence: list[dict]

    open_questions: list[str]

    draft: str

    verification_passed: bool
    verification_feedback: str

    iteration: int

    context_stats: dict
```

其中：

```text
messages
```

仍然保留，作为原始执行日志 / Trace 来源。

但是：

> V4 不要求每个 Node 都把全部 messages 发给 LLM。

---

# 6. 核心模块：Context Manager

新增：

```text
context/
├── manager.py
├── selector.py
├── compressor.py
├── budget.py
└── metrics.py
```

核心类：

```python
class ContextManager:

    def build_planner_context(self, state):
        ...

    def build_researcher_context(self, state):
        ...

    def build_writer_context(self, state):
        ...

    def build_verifier_context(self, state):
        ...

    def select_evidence(self, state, query, top_k):
        ...

    def compress_tool_result(self, result):
        ...

    def estimate_tokens(self, context):
        ...
```

---

# 7. 第一项：Node-specific Context

这是 V4 最重要的优化之一。

不同 Node 不需要看相同信息。

---

## 7.1 Planner Context

Planner 只需要：

```text
User Query
+
任务约束
```

不需要：

```text
全部 Tool Results
全部 Evidence
旧 Draft
```

建议：

```text
Planner Context:

- 用户问题
- 系统规则
- 必要约束
```

---

## 7.2 Researcher Context

Researcher 需要：

```text
User Query
+
Current Plan
+
Current Step
+
已有 Evidence 摘要
+
Open Questions
+
Verifier Feedback
```

不需要：

```text
完整旧 Draft
全部历史对话
全部 Raw Tool Output
```

---

## 7.3 Writer Context

Writer 只需要：

```text
User Query
+
Plan
+
Relevant Evidence
+
必要来源信息
```

Writer 不需要：

```text
Tool 调用过程
Tool Error
搜索失败日志
Researcher 的内部步骤
```

---

## 7.4 Verifier Context

Verifier 只需要：

```text
User Query
+
Draft
+
Relevant Evidence
+
任务要求
```

不需要：

```text
全部 Planner Trace
全部 Tool Result
```

---

# 8. 第二项：Tool Result Compression

Tool 常常返回大段内容。

例如：

```text
web_search
→ 10 个网页
→ 5000 tokens

local_search
→ 5 个 chunks
→ 3500 tokens

paper_search
→ 5 篇论文摘要
→ 3000 tokens
```

这些内容不能长期全部保留在 Active Context。

目标：

```text
Raw Tool Result
      ↓
Evidence Extractor / Compressor
      ↓
结构化 Evidence
```

建议格式：

```python
{
    "claim": "...",
    "source_type": "local_search",
    "source": "vjepa2.pdf",
    "page": 7,
    "url": None,
    "evidence_text": "...",
    "relevance": 0.88,
    "query": "..."
}
```

---

# 9. Tool Result Compression 的实现方式

第一版采用两级方案。

## Level 1：规则压缩

优先使用普通 Python，不调用 LLM。

例如：

```text
限制每个结果长度
去除重复字段
删除无关 metadata
删除过长 abstract
只保留 source / page / title / snippet
```

例如：

```text
原始 Tool Result：
2800 tokens

规则压缩：
900 tokens
```

---

## Level 2：LLM Evidence Extraction

仅在 Tool Result 过长时触发。

例如：

```text
Tool Result > 2500 tokens
```

执行：

```text
Raw Result
↓
LLM Extractor
↓
关键结论 + Evidence + Source
```

Extractor 不负责回答用户。

只负责：

```text
提取与当前 Research Step 有关的信息
```

---

# 10. LLM Evidence Extractor Prompt

目标不是生成 Summary，而是提取 Evidence。

例如：

```text
输入：

Current Research Question:
...

Raw Tool Result:
...

输出：

1. 支持当前问题的关键事实
2. 每个事实对应的来源
3. 证据原文片段
4. 如果内容无关，返回空
```

要求：

```text
不得自行补充外部知识
不得编造来源
不得改变页码 / URL
```

---

# 11. 第三项：Evidence Selection

即使 Tool Result 被压缩后，Evidence 仍可能越来越多。

例如：

```text
Evidence Count:
120
```

Writer 没必要看到全部 120 条。

所以增加：

```text
Evidence Selector
```

根据当前 Node 的任务选择最相关 Evidence。

---

# 12. Evidence Selection 第一版方案

优先复用已有 Embedding / FAISS 思路。

区别：

V2：

```text
Query
→ 检索 PDF Chunk
```

V4：

```text
Current Step / Current Draft
→ 检索 Agent 已收集的 Evidence
```

可以先简单实现：

```text
Evidence
↓
Embedding
↓
临时向量索引
↓
Top-K
```

或者 Evidence 数量较小时直接：

```text
Embedding Matrix
+
Cosine Similarity
```

不强制再建独立 FAISS 文件。

---

# 13. Evidence Selection 输入

不同 Node 使用不同检索 Query。

### Researcher

```text
Current Plan Step
+
Open Question
```

### Writer

```text
User Query
+
当前要回答的主题
```

### Verifier

```text
Draft 中的关键 Claim
+
User Query
```

---

# 14. Evidence Top-K

第一版建议：

```text
Researcher:
Top-K = 5~8

Writer:
Top-K = 8~12

Verifier:
Top-K = 8~12
```

不要写死在代码中。

使用配置：

```python
CONTEXT_CONFIG = {
    "researcher_top_k": 6,
    "writer_top_k": 10,
    "verifier_top_k": 10,
}
```

---

# 15. 第四项：Context Budget

V4 必须增加 Context Budget。

例如：

```text
每个 Node 最大输入：
8000 tokens
```

注意：

> 不要求一定使用 8000，只要求建立预算机制。

推荐配置：

```python
CONTEXT_BUDGET = {
    "planner": 3000,
    "researcher": 6000,
    "writer": 8000,
    "verifier": 8000,
}
```

---

# 16. Token 估算

优先使用当前 LLM 对应 tokenizer。

如果暂时难以获得：

```text
可以先使用近似 token 估计
```

例如：

```text
英文：
characters / 4

中文：
characters / 1.5 ~ 2
```

但最终 Evaluation 中必须注明估算方式。

---

# 17. 超过 Context Budget 后的处理顺序

不要简单从头截断。

推荐优先级：

```text
1. 删除重复 Evidence

2. 删除低相关 Evidence

3. 压缩 Tool Result

4. 删除无关历史 Message

5. 保留：
   Query
   Current Step
   Open Questions
   Verifier Feedback

6. 仍然超限时再截断低优先级内容
```

---

# 18. 信息优先级

定义：

```text
P0：绝不能丢

User Query
Current Plan Step
关键任务约束

P1：高优先级

Verifier Feedback
Open Questions
高相关 Evidence

P2：中优先级

辅助 Evidence
Plan 其他步骤

P3：低优先级

旧 Tool Trace
重复 Evidence
失败 Tool Result
旧的中间 Draft
```

Context Manager 按优先级裁剪。

---

# 19. 不做“跨会话历史摘要”

V4 明确删除以下设计：

```text
Conversation Summary
Long-term Chat Memory
用户多轮会话持久化
跨任务历史检索
```

原因：

当前 Research Agent 的主要目标是：

```text
单个复杂任务的执行优化
```

而不是：

```text
长期陪伴式聊天
```

因此 V4 不做：

```text
“上次你说过什么”
```

类型的 Memory。

---

# 20. Open Questions 必须显式维护

上下文压缩最容易出现的问题：

```text
Agent 记得已经知道什么
但忘了还有什么没查
```

因此必须维护：

```python
open_questions = [
    "...",
    "...",
]
```

Verifier Fail 时可以新增：

```text
缺少 DINOv3 robotics benchmark
```

Researcher 下一轮优先解决。

---

# 21. Evidence 去重

多个 Worker / Tool 可能返回相同信息。

V4 加入基础去重。

第一版可以：

```text
相同 source + page
→ 合并

高度相似 evidence_text
→ 只保留一条
```

如果使用 embedding：

```text
cosine similarity > threshold
→ 认为高度重复
```

阈值先作为配置，不做复杂调优。

---

# 22. V4 运行流程

最终：

```text
User Query
    ↓
Router / Planner
    ↓
Researcher
    ↓
Tool
    ↓
Raw Tool Result
    ↓
Tool Result Compressor
    ↓
Evidence Pool
    ↓
Evidence Dedup
    ↓
Context Manager
    ↓
Relevant Evidence Top-K
    ↓
Writer
    ↓
Verifier
```

每一个需要 LLM 的 Node 前：

```text
State
↓
Context Manager
↓
Node-specific Context
↓
LLM
```

---

# 23. V3 Baseline 必须保留

V3：

```text
Full Context
```

尽可能保留当前方式：

```text
messages / evidence 全量传递
```

V4：

```text
Compressed / Selected Context
```

两个版本必须同时存在。

---

# 24. V4 的核心测试问题

不能只验证：

```text
“能不能跑”
```

必须验证：

> **上下文压缩是否降低成本，同时保持任务质量。**

---

# 25. 测试集分类

至少设计三类长度。

## A. 短任务

目标：

```text
5 步以内
```

例如：

```text
根据本地论文解释 V-JEPA 2-AC。
```

预期：

```text
V3 和 V4 差异不应该很大
```

---

## B. 中任务

目标：

```text
10~15 步
```

例如：

```text
比较 V-JEPA 2 与 DINOv3
在视觉表征和机器人控制上的区别。
```

---

## C. 长任务

目标：

```text
20~30 步
```

例如：

```text
比较 V-JEPA 2、DINOv3、JEPA-WAM、Patch Policy，
从训练目标、视觉表征、动作建模、机器人泛化、
开源情况五个方面进行系统分析。
```

长任务是 V4 的重点测试对象。

---

# 26. V3 vs V4 对照指标

必须统计：

| Metric | V3 Full Context | V4 Context Manager |
|---|---:|---:|
| Task Success Rate | | |
| Final Answer Accuracy | | |
| Input Tokens | | |
| Output Tokens | | |
| Total Tokens | | |
| Average Context Tokens / Step | | |
| Max Context Tokens | | |
| LLM Calls | | |
| Tool Calls | | |
| Repeated Tool Calls | | |
| Retrieval Calls | | |
| Total Latency | | |
| Average LLM Latency | | |
| Prefill Latency（如可获取） | | |
| Evidence Count | | |
| Verifier Pass Rate | | |

---

# 27. Context Growth 曲线

必须记录每一步：

```text
Step
Input Tokens
Evidence Count
Tool Calls
Latency
```

最终画：

```text
Context Tokens
↑
│
│              V3
│             /
│            /
│           /
│          /
│     V4 ─────────
│
└──────────────────→ Agent Steps
```

目标是观察：

```text
V3 Context 是否持续增长

V4 是否保持相对稳定
```

---

# 28. V4 消融实验

至少做以下四组：

## A：V3 Baseline

```text
Full Context
```

---

## B：只做 Tool Result Compression

```text
Raw Tool Result
→ Compressed Evidence
```

不做 Evidence Top-K。

---

## C：Tool Result Compression + Evidence Top-K

```text
Compression
+
Evidence Selection
```

---

## D：完整 V4

```text
Tool Result Compression
+
Evidence Top-K
+
Node-specific Context
+
Context Budget
```

---

# 29. 消融实验回答的问题

最终需要回答：

```text
1. Tool Result Compression 单独能省多少 Token？

2. Evidence Top-K 是否进一步降低上下文？

3. Node-specific Context 是否减少无关信息？

4. 哪个模块对延迟改善最大？

5. 哪个模块最容易导致准确率下降？
```

---

# 30. 压缩质量测试

不能只测 Token。

还需要检查：

```text
压缩后关键事实是否丢失
来源是否保留
页码是否保留
Verifier Feedback 是否保留
Open Questions 是否保留
```

设计人工检查表：

| Item | Preserved |
|---|---:|
| Main claim | Yes/No |
| Source | Yes/No |
| Page / URL | Yes/No |
| Open Question | Yes/No |
| Critical constraint | Yes/No |

---

# 31. Failure Case

必须专门测试：

## Case 1：压缩过度

Evidence 被删太多：

```text
Task Success 明显下降
```

记录失败原因。

---

## Case 2：重复搜索

如果压缩后 Agent 忘记：

```text
已经查过某个问题
```

可能重复 Tool Call。

记录：

```text
Repeated Tool Calls
```

---

## Case 3：Verifier 信息丢失

Verifier 给出：

```text
缺少 DINOv3 robotics evidence
```

压缩后 Researcher 必须仍然看到这个 Feedback。

---

## Case 4：来源丢失

Evidence 压缩后不得出现：

```text
只剩结论
没有 source / page / URL
```

---

# 32. 目标结果

V4 不要求一定达到某个数字。

理想结果类似：

```text
平均 Input Tokens ↓ 30~50%

长任务最大 Context ↓

Prefill / Total Latency ↓

Repeated Tool Calls 不增加

Task Success 基本不下降
```

重点是：

```text
压缩收益
vs
任务质量损失
```

---

# 33. 如果 V4 准确率下降怎么办

不要为了压 Token 强行保留方案。

如果：

```text
V3 Success = 90%
V4 Success = 65%
```

说明压缩策略失败。

需要分析：

```text
Evidence Top-K 太小？
Tool Result 压缩过度？
Context Budget 太低？
Verifier Feedback 丢失？
Node-specific Context 缺重要字段？
```

---

# 34. 配置文件

建议：

```python
CONTEXT_CONFIG = {
    "enabled": True,

    "researcher_top_k": 6,
    "writer_top_k": 10,
    "verifier_top_k": 10,

    "planner_budget": 3000,
    "researcher_budget": 6000,
    "writer_budget": 8000,
    "verifier_budget": 8000,

    "tool_result_compress_threshold": 2500,

    "dedup_similarity_threshold": 0.92,
}
```

所有关键参数可修改。

禁止大量 magic number 散落代码。

---

# 35. Trace 新增内容

V4 Trace 至少输出：

```text
[Context Manager]

Node:
Writer

Raw Context Tokens:
10432

Selected Evidence:
10 / 47

Compressed Tool Results:
3

Final Context Tokens:
4218

Compression Ratio:
59.6%
```

还需要记录：

```text
Dropped Evidence Count
Deduplicated Evidence Count
Context Budget
```

---

# 36. 推荐目录

```text
Research_Agent/
│
├── agent/
│   ├── legacy/
│   ├── graph/
│   │   └── ...
│   │
│   └── context/
│       ├── manager.py
│       ├── selector.py
│       ├── compressor.py
│       ├── budget.py
│       └── metrics.py
│
├── evaluation/
│   ├── v3_results.md
│   ├── v4_results.md
│   ├── v4_ablation.md
│   └── v4_traces.md
│
└── tests/
    ├── test_context_manager.py
    ├── test_evidence_selector.py
    └── test_compressor.py
```

不强制移动原有目录。

原则：

```text
优先不破坏旧代码。
```

---

# 37. 单元测试

至少新增：

```text
test_context_manager.py
test_evidence_selector.py
test_compressor.py
```

测试：

```text
Context 不超过 Budget

P0 信息不会被删除

Evidence Top-K 数量正确

Tool Result 压缩后来源仍存在

Verifier Feedback 不丢失

Open Questions 不丢失

Evidence 去重有效
```

---

# 38. 回归测试

实现 V4 后必须重新验证：

```text
Legacy ReAct
V3 Graph Baseline
V4 Context Graph
```

旧版本全部能运行。

禁止：

```text
为了 V4 修改旧接口导致 V3 跑不起来
```

---

# 39. 完成后需要提交

本地 Agent 完成 V4 后输出：

```text
1. 最终目录树

2. Context Manager 结构

3. Node-specific Context 设计

4. Tool Result Compression 方案

5. Evidence Selection 方案

6. Context Budget 方案

7. Evidence Dedup 方案

8. V3 Regression Test

9. V3 vs V4 对照结果

10. 消融实验结果

11. Context Growth 曲线

12. 至少 3 条完整 V4 Trace

13. 至少 3 个 Failure Case

14. 当前已知问题

15. 对 V5 的建议
```

---

# 40. V4 必须回答的问题

完成后需要能够解释：

1. 为什么不能无限堆 messages？
2. Context 长度为什么会影响 Prefill？
3. 为什么不同 Node 不需要看到相同 Context？
4. Tool Result Compression 解决什么问题？
5. 为什么 Evidence 需要结构化？
6. Evidence Top-K 和 V2 RAG 有什么相似之处？
7. 为什么不做跨会话 Memory？
8. Context Budget 是什么？
9. 哪些信息绝不能被压掉？
10. 为什么压缩率不是越高越好？
11. 如何判断上下文压缩是否真正有效？
12. 为什么必须做 V3 Baseline？
13. 消融实验的意义是什么？
14. 如果 Token 降低但 Task Success 大幅下降，方案是否成功？

第 14 个问题：

```text
不是。
```

V4 的目标是：

> **在尽量保持任务质量的前提下，减少无关上下文和重复信息，从而降低输入 Token、Prefill 开销和长任务中的上下文干扰。**

---

# 41. V4 最终架构

```text
                    Research State
                          │
                          ▼
                   Context Manager
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
 Tool Result         Evidence           Context
 Compression         Selection           Budget
        │                 │                 │
        └─────────────────┼─────────────────┘
                          ▼
                Node-specific Context
                          │
          ┌───────────────┼────────────────┐
          ▼               ▼                ▼
       Planner        Researcher      Writer / Verifier
```

Tool 侧：

```text
Tool
 ↓
Raw Result
 ↓
Compression
 ↓
Structured Evidence
 ↓
Evidence Pool
 ↓
Top-K Selection
 ↓
Current Node
```

---

# 42. V4 核心价值

V3 解决：

```text
复杂 Agent 的 Workflow 如何显式组织
```

V4 解决：

```text
复杂 Agent 跑得越来越久时，
如何避免 Context 无限增长
```

最终项目需要能够证明：

> **Context Manager 不是为了“加一个 Memory 功能”，而是为了让长链路 Agent 只保留当前任务所需的最小充分信息，并通过实验验证这种压缩是否降低 Token 和延迟，同时维持任务成功率。**
