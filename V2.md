# Research Agent V2：本地论文 RAG 接入任务书

## 1. 项目目标

在已经跑通的 V0 Tool-Calling Agent 基础上，加入一个最基础但完整的本地论文 RAG 能力。

目标是让 Agent 能够读取本地 `papers/` 目录中的 arXiv PDF，并通过语义检索找到相关段落，再将检索结果作为 Tool Observation 返回给 LLM。

最终链路：

```text
本地 PDF
   ↓
PDF Parser
   ↓
Text
   ↓
Chunk
   ↓
Embedding Model
   ↓
Vector
   ↓
FAISS Index
```

查询时：

```text
User Query
   ↓
LLM
   ↓
判断需要查询本地论文
   ↓
local_search Tool
   ↓
Query Embedding
   ↓
FAISS Retrieval
   ↓
Top-K Chunks
   ↓
Observation
   ↓
LLM
   ↓
Final Answer
```

本版本的重点不是优化 RAG，而是把完整的 RAG Pipeline 搭起来。

---

## 2. 本阶段核心学习目标

完成 V2 后，应理解以下概念：

1. PDF Parser 是什么
2. 为什么 PDF 不能直接送给 Embedding Model
3. Chunking 是什么
4. 为什么需要 Chunk Overlap
5. Embedding Model 是什么
6. Query Embedding 和 Document Embedding 为什么必须在同一空间
7. 为什么 Embedding Model 不需要和 LLM Hidden Space 对齐
8. Vector Database / Vector Index 是什么
9. Similarity Search 是什么
10. Retriever 是什么
11. RAG 是什么
12. RAG 和 Tool Calling 是什么关系
13. 为什么文档 Embedding 应该离线计算
14. 为什么每次 Query 只需要重新计算 Query Embedding
15. RAG 检索结果为什么要保留 source/page metadata

---

## 3. 本阶段不做什么

V2 暂时不做：

- LangChain
- LangGraph
- MCP
- Memory
- Multi-Agent
- GraphRAG
- Hybrid Search
- BM25
- Reranker
- Query Rewrite
- Multi-Query Retrieval
- Agentic RAG
- OCR
- 多模态 PDF 理解
- 表格结构化解析
- 复杂公式解析
- Web UI
- Milvus / Elasticsearch 等大型数据库

本阶段只实现：

```text
PDF
→ Text
→ Chunk
→ Embedding
→ FAISS
→ Retriever
→ local_search Tool
→ Agent
```

---

## 4. 推荐技术栈

建议使用：

```text
Python >= 3.10

PDF Parser:
PyMuPDF

Embedding:
BAAI/bge-small-en-v1.5

Vector Index:
FAISS

Model:
sentence-transformers

其他:
numpy
python-dotenv
```

如果当前机器对 Hugging Face 下载受限，可以允许替换成其他本地可用 Embedding Model。

但必须保证：

```text
Document Embedding
和
Query Embedding
```

由同一个模型生成。

---

## 5. 为什么选择 BGE Small

本阶段优先使用：

```text
BAAI/bge-small-en-v1.5
```

原因：

- arXiv 论文主要是英文
- 模型较小
- 本地部署方便
- CPU 也能运行
- 足够完成基础语义检索
- V2 重点是跑通 Pipeline，不是追求最高 Retrieval Accuracy

后续如果需要，可以再替换：

```text
BGE-M3
E5
GTE
```

进行效果比较。

---

## 6. 项目目录

在已有 Research Agent 项目中加入：

```text
research-agent/
│
├── main.py
├── README.md
├── requirements.txt
├── .env.example
│
├── agent/
│   ├── __init__.py
│   ├── agent.py
│   └── llm.py
│
├── tools/
│   ├── __init__.py
│   ├── calculator.py
│   ├── web_search.py
│   ├── paper_search.py
│   └── local_search.py
│
├── prompts/
│   ├── __init__.py
│   └── system_prompt.py
│
├── papers/
│   ├── example_1.pdf
│   ├── example_2.pdf
│   └── ...
│
├── rag/
│   ├── __init__.py
│   ├── pdf_parser.py
│   ├── chunker.py
│   ├── embedding.py
│   ├── vector_store.py
│   ├── retriever.py
│   └── ingest.py
│
├── data/
│   └── vector_db/
│       ├── index.faiss
│       └── metadata.json
│
└── tests/
    ├── test_chunker.py
    ├── test_retriever.py
    └── test_local_search.py
```

---

## 7. PDF 输入方式

把本地 arXiv PDF 直接复制到：

```text
papers/
```

例如：

```text
papers/
├── vjepa2.pdf
├── jepa_wam.pdf
├── patch_policy.pdf
└── lawam.pdf
```

要求：

- 自动扫描 `papers/*.pdf`
- 支持多个 PDF
- 每篇 PDF 分别记录文件名
- 不要求 OCR
- 默认只支持有文本层的 PDF

如果 PDF 无法提取文本：

```text
Warning:
No extractable text found in xxx.pdf
```

并跳过。

---

## 8. PDF Parser

文件：

```text
rag/pdf_parser.py
```

使用：

```python
import fitz
```

也就是 PyMuPDF。

接口建议：

```python
def parse_pdf(path: str) -> list[dict]:
    ...
```

返回：

```python
[
    {
        "source": "vjepa2.pdf",
        "page": 1,
        "text": "..."
    },
    {
        "source": "vjepa2.pdf",
        "page": 2,
        "text": "..."
    }
]
```

要求：

- 按页提取
- 保留页码
- 清理明显多余空白
- 不需要复杂 Layout Parsing
- 空页面跳过

---

## 9. Chunking

文件：

```text
rag/chunker.py
```

目标：将长文本切成适合 Embedding 和 Retrieval 的小块。

基础参数：

```text
chunk_size ≈ 500 tokens
chunk_overlap ≈ 50 tokens
```

如果实现 token-based chunking 较复杂，可以先采用字符近似：

```text
chunk_size ≈ 2000 characters
overlap ≈ 200 characters
```

但 README 中必须注明这是近似方案。

推荐接口：

```python
def chunk_pages(
    pages: list[dict],
    chunk_size: int = 500,
    overlap: int = 50
) -> list[dict]:
    ...
```

输出：

```python
[
    {
        "chunk_id": 0,
        "source": "vjepa2.pdf",
        "page": 7,
        "text": "..."
    }
]
```

---

## 10. 为什么需要 Chunk

不要把整篇论文直接做成一个 Embedding。

原因：

```text
整篇论文
→ 信息太杂
→ 一个向量难以表示所有局部语义
→ Retrieval 粒度太粗
```

正确做法：

```text
论文
↓
多个语义片段
↓
每个片段一个 Embedding
```

---

## 11. 为什么需要 Overlap

如果完全硬切：

```text
Chunk A:
V-JEPA 2-AC is trained using

Chunk B:
DROID robot interaction data...
```

关键语义可能被拆开。

加入 overlap 后可减少边界切断问题。

---

## 12. Embedding Model

文件：

```text
rag/embedding.py
```

使用：

```python
from sentence_transformers import SentenceTransformer
```

接口：

```python
class EmbeddingModel:

    def encode_documents(
        self,
        texts: list[str]
    ):
        ...

    def encode_query(
        self,
        query: str
    ):
        ...
```

文档：

```text
Chunk Text
   ↓
BGE
   ↓
Embedding Vector
```

查询：

```text
User Query
   ↓
同一个 BGE
   ↓
Query Vector
```

---

## 13. 重要概念：Embedding 不需要和 LLM 对齐

Embedding Model 的输出不会直接输入 LLM Hidden State。

实际流程：

```text
Query
 ↓
Embedding Model
 ↓
Query Vector

Document
 ↓
Embedding Model
 ↓
Document Vector

两者做相似度搜索
 ↓
找到相关 Chunk
 ↓
取出原始 Chunk Text
 ↓
把文本送给 LLM
```

因此必须对齐的是：

```text
Query Embedding
↔
Document Embedding
```

但不需要：

```text
Embedding Space
↔
LLM Hidden Space
```

---

## 14. Vector Store

文件：

```text
rag/vector_store.py
```

使用：

```text
FAISS
```

需要保存：

```text
index.faiss
metadata.json
```

FAISS 保存 Embedding Vectors，`metadata.json` 保存：

```json
[
  {
    "chunk_id": 0,
    "source": "vjepa2.pdf",
    "page": 7,
    "text": "..."
  }
]
```

要求 FAISS 第 N 个向量必须对应 `metadata[N]`。

---

## 15. 为什么使用 FAISS

V2 不需要真正的数据库服务器。

FAISS 足以完成：

```text
Vector Storage
+
Nearest Neighbor Search
```

优势：

- 简单
- 本地运行
- 无服务依赖
- 适合当前实验项目
- 便于理解 Retrieval 本身

---

## 16. Ingestion Pipeline

文件：

```text
rag/ingest.py
```

执行：

```bash
python -m rag.ingest
```

完整流程：

```text
扫描 papers/
   ↓
读取 PDF
   ↓
提取每页文本
   ↓
Chunk
   ↓
Batch Embedding
   ↓
建立 FAISS Index
   ↓
保存 index.faiss
   ↓
保存 metadata.json
```

示例输出：

```text
[RAG] Found 4 PDFs
[RAG] Total chunks: 326
[RAG] Encoding chunks...
[RAG] Saving FAISS index...
Done.
```

---

## 17. Ingestion 必须离线执行

不能每次：

```bash
python main.py
```

都重新解析全部论文并重新计算 Embedding。

正确设计：

```text
第一次添加文档
→ python -m rag.ingest
→ PDF → Embedding → FAISS

正常启动 Agent
→ python main.py
→ 只加载 index.faiss + metadata.json
```

---

## 18. Retriever

文件：

```text
rag/retriever.py
```

接口：

```python
class Retriever:

    def search(
        self,
        query: str,
        top_k: int = 5
    ) -> list[dict]:
        ...
```

流程：

```text
query
 ↓
Embedding Model
 ↓
query vector
 ↓
FAISS.search()
 ↓
Top-K Index IDs
 ↓
metadata
 ↓
Top-K Chunks
```

返回：

```python
[
    {
        "score": 0.83,
        "source": "vjepa2.pdf",
        "page": 7,
        "text": "..."
    }
]
```

---

## 19. Similarity Search

本阶段只实现最基础的：

```text
Top-K Semantic Similarity Search
```

可以使用：

```text
Cosine Similarity
```

或者将向量 normalize 后使用 FAISS Inner Product。

README 中说明采用的相似度方法。

---

## 20. local_search Tool

文件：

```text
tools/local_search.py
```

接口：

```python
def local_search(
    query: str,
    top_k: int = 5
) -> str:
    ...
```

Tool Schema：

```python
{
    "type": "function",
    "function": {
        "name": "local_search",
        "description": "Search the local academic paper knowledge base.",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string"
                },
                "top_k": {
                    "type": "integer",
                    "default": 5
                }
            },
            "required": ["query"]
        }
    }
}
```

---

## 21. local_search 输出格式

建议：

```text
[Result 1]

Source:
vjepa2.pdf

Page:
7

Score:
0.83

Text:
We train the action-conditioned predictor...
```

必须保留：

```text
source
page
text
```

---

## 22. 注册到 Tool Registry

V2：

```python
TOOL_REGISTRY = {
    "calculator": calculator,
    "web_search": web_search,
    "paper_search": paper_search,
    "local_search": local_search,
}
```

Schema 同样加入 `TOOL_SCHEMAS`。

---

## 23. System Prompt 更新

增加规则：

```text
Use local_search when the user asks questions
that should be answered from locally stored papers
or explicitly refers to local documents.

Prefer local_search over web_search when the user
asks to answer based on the local paper library.

Do not fabricate information that is not present
in the retrieved local context.
```

---

## 24. Trace 要求

V2 Trace 至少输出：

```text
[Agent] Step 1

[LLM]
Tool Call:
local_search

Arguments:
{
    "query": "...",
    "top_k": 5
}

[RAG]
Query embedding generated

[RAG]
Top-K retrieval complete

[Observation]
Result 1: ...
Result 2: ...

[Agent] Step 2

[Final Answer]
...
```

不需要输出完整向量。

---

## 25. 测试 Case

### Case 1：单论文事实查询

```text
根据本地论文，
V-JEPA 2-AC 使用了什么机器人数据？
```

要求调用 `local_search`。

### Case 2：方法查询

```text
根据本地论文，
V-JEPA 2 的 predictor 是如何训练的？
```

检查 Retrieval 是否找到 Method / Training 相关段落。

### Case 3：跨论文查询

```text
根据本地论文，
比较 V-JEPA 2 和 JEPA-WAM 的主要区别。
```

允许 Agent 多次调用 `local_search`。

### Case 4：不存在的信息

```text
根据本地论文，
V-JEPA 3 的机器人实验结果是多少？
```

如果资料库不存在，不得编造。

### Case 5：工具选择

```text
帮我查一下今天 V-JEPA 2 有没有新的官方消息。
```

期望使用 `web_search`，用于验证 Agent 是否能区分本地知识与最新互联网信息。

---

## 26. 基础 Evaluation

至少记录：

| Query | Correct Source | Correct Page Area | Relevant Top-K | Final Answer Correct |
|---|---:|---:|---:|---:|
| Query 1 | Yes/No | Yes/No | Yes/No | Yes/No |
| Query 2 | Yes/No | Yes/No | Yes/No | Yes/No |

额外记录：

```text
Embedding Model
PDF Count
Chunk Count
Average Retrieval Latency
Average Agent Latency
```

---

## 27. Offline / Online Cost 区分

Offline：

```text
PDF Parsing
Chunking
Document Embedding
FAISS Index Building
```

Online：

```text
Query Embedding
FAISS Search
LLM Prefill
LLM Decode
```

正常 Query 不应该重新计算所有 Document Embedding。

---

## 28. 错误处理

至少处理：

### Vector DB 不存在

```text
ToolError:
Local vector database not found.
Run rag/ingest.py first.
```

### Papers 目录为空

```text
No PDF files found in papers/
```

### PDF 无文本层

```text
Warning:
No extractable text found.
```

并跳过。

### Embedding Model 加载失败

返回清晰错误。

### FAISS Search 失败

不允许整个 Agent 直接崩溃。

---

## 29. 代码质量要求

必须做到：

- Parser / Chunker / Embedding / Retriever 分模块
- 不把 RAG 全写进 `local_search.py`
- Document Embedding 支持 batch
- metadata 和 FAISS index 一一对应
- 本地向量库持久化
- Tool Error 不导致 Agent 退出
- 有 Trace
- 有单元测试
- 有 README
- 不提交 API Key

---

## 30. 验收标准

### PDF Pipeline

- [ ] 能扫描 `papers/`
- [ ] 能解析 arXiv PDF 文本
- [ ] 能按页保留 metadata
- [ ] 能切 Chunk
- [ ] Chunk 有 overlap
- [ ] 能批量生成 Embedding
- [ ] 能创建 FAISS Index
- [ ] 能保存 Index
- [ ] 能保存 metadata

### Retrieval

- [ ] Query 能生成 Embedding
- [ ] 能执行 Top-K Search
- [ ] 能返回原始文本
- [ ] 能返回 source
- [ ] 能返回 page
- [ ] Retrieval 不需要重新 encode 全部 PDF

### Agent Integration

- [ ] 新增 `local_search`
- [ ] Tool Schema 注册成功
- [ ] Agent 能自主调用 local_search
- [ ] Observation 能正确返回 LLM
- [ ] Agent 能基于检索文本回答
- [ ] Agent 能区分 local_search 和 web_search

### Robustness

- [ ] Vector DB 缺失时不崩溃
- [ ] PDF 无文本时不崩溃
- [ ] Embedding 错误有明确提示
- [ ] Retrieval 错误有明确提示

---

## 31. 完成后需要提交的结果

本地 Agent 完成后输出：

```text
1. 最终目录树
2. 使用的 PDF Parser
3. 使用的 Embedding Model
4. Chunk Size / Overlap
5. PDF 数量
6. 总 Page 数
7. 总 Chunk 数
8. Embedding 耗时
9. FAISS Index 大小
10. Retriever 核心代码
11. local_search Tool Schema
12. Agent Tool Registry 更新
13. 至少 5 个 Retrieval Query 的结果
14. 至少 3 个完整 Agent Trace
15. 测试结果
16. 当前存在的问题
```

---

## 32. 必须实际测试的问题

```text
Q1:
根据本地论文，
V-JEPA 2 的核心训练目标是什么？

Q2:
根据本地论文，
V-JEPA 2-AC 是如何使用 action 的？

Q3:
根据本地论文，
机器人控制部分使用了什么训练数据？

Q4:
比较本地论文中
V-JEPA 2 和其他 JEPA-style 方法的区别。

Q5:
根据本地资料，
给出支持你回答的论文名和页码。
```

如果实际 PDF 中不存在对应信息，可以替换成与已有论文匹配的问题。

---

## 33. 本阶段最终系统

```text
                     Research Agent V2

User
 │
 ▼
LLM
 │
 ├──────── calculator
 │
 ├──────── web_search
 │
 ├──────── paper_search
 │
 └──────── local_search
              │
              ▼
          Retriever
              │
              ▼
       Query Embedding
              │
              ▼
            FAISS
              │
              ▼
         Top-K Chunks
              │
              ▼
          Observation
              │
              └────────→ LLM
```

离线知识库：

```text
papers/
   │
   ▼
PDF Parser
   │
   ▼
Chunker
   │
   ▼
Embedding Model
   │
   ▼
FAISS + Metadata
```

---

## 34. V2 的核心价值

V0 解决的是：

```text
LLM 怎么调用外部 Tool
```

V2 解决的是：

```text
Agent 怎么使用自己的本地知识库
```

也就是从：

```text
LLM + Tools
```

扩展到：

```text
LLM
+
External Tools
+
Private / Local Knowledge
```

本阶段完成后，再继续进入 Workflow / LangGraph，而不是现在就开始做复杂 RAG 优化。
